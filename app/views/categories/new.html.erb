<%= render 'errors' %>
<h1>Create a New Category</h1>

<%= form_for @category do |f| %>
  <div class="field<%= ' field_with_errors' if @category.errors[:name].any? %>">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit %>
<% end %>